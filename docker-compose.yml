services:
  server:
    build:
      context: .
    ports:
      - 8080:8080
    depends_on:
      - postgres

  postgres:
    image: postgres:latest
    environment:
      POSTGRES_USER: employee
      POSTGRES_PASSWORD: tracker
      POSTGRES_DB: employee_dar_db
    ports:
      - "5432:5432"